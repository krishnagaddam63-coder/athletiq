services:
  - type: web
    name: athletiq
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: athletiq-db
          property: connectionString

databases:
  - name: athletiq-db
    plan: free
